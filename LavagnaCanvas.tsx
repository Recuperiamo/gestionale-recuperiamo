// Commented out to disable automatic spectator mode activation
// setSpectatorMode(online)

if (!spectatorModeRef.current) setSpectatorMode(true) // Commented out to disable automatic spectator mode activation

// (rest of the code unchanged)