# Cartella `src/fetcher/`

Qui vanno le funzioni che si occupano di chiamare le API dal frontend (fetch, axios, ecc.).  
Mantieni separate le funzioni di recupero o invio dati rispetto ai componenti UI.

Aggiungi qui solo utility per comunicare con endpoint API.