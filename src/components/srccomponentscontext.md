# Cartella `src/components/`

Raccoglie tutti i componenti React riusabili dell’interfaccia utente (UI), come form, tabelle, modali, CRUD, ecc.

Aggiungi qui solo componenti che possono essere importati in più pagine o sezioni.