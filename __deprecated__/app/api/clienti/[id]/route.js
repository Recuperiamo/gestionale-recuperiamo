// Deprecated: placeholder implementation moved here on 2025-10-28
// Original placeholder API for cliente detail (GET/PUT/DELETE)
export async function GET(req) {
  return new Response(JSON.stringify({ message: "GET dettaglio cliente - da implementare (moved to __deprecated__)" }), { status: 200 });
}

export async function PUT(req) {
  return new Response(JSON.stringify({ message: "PUT dettaglio cliente - da implementare (moved to __deprecated__)" }), { status: 200 });
}

export async function DELETE(req) {
  return new Response(JSON.stringify({ message: "DELETE dettaglio cliente - da implementare (moved to __deprecated__)" }), { status: 200 });
}
