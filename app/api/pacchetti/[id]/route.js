// API route per operazioni su singolo pacchetto ore (dettaglio, update, delete)
export async function GET(req) {
  return new Response(JSON.stringify({ message: "GET dettaglio pacchetto ore - da implementare" }), { status: 200 });
}

export async function PUT(req) {
  return new Response(JSON.stringify({ message: "PUT dettaglio pacchetto ore - da implementare" }), { status: 200 });
}

export async function DELETE(req) {
  return new Response(JSON.stringify({ message: "DELETE dettaglio pacchetto ore - da implementare" }), { status: 200 });
}