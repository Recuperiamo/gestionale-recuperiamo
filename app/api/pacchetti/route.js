// API route principale per CRUD pacchetti ore (bozza vuota)
// Da completare con logica POST, GET, PUT, DELETE
export async function GET(req) {
  return new Response(JSON.stringify({ message: "GET pacchetti ore - da implementare" }), { status: 200 });
}

export async function POST(req) {
  return new Response(JSON.stringify({ message: "POST pacchetto ore - da implementare" }), { status: 201 });
}

export async function PUT(req) {
  return new Response(JSON.stringify({ message: "PUT pacchetto ore - da implementare" }), { status: 200 });
}

export async function DELETE(req) {
  return new Response(JSON.stringify({ message: "DELETE pacchetto ore - da implementare" }), { status: 200 });
}